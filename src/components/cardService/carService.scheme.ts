import { ImageSourcePropType } from "react-native"

export interface CardServiceProps  {
    color: string
    title?: string
    hours?: number
    minutes?: number
    value?: number
    cardImage?: string
}